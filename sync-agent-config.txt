# ============================================================
# Brandigade HRIS - sync-agent configuration
# Office laptop, file: sync-agent/.env
#
# Verified against the live server on 2026-09-01: this token is
# accepted by https://hris.brandigade.com/api (returned HTTP 200).
#
# TREAT AS A PASSWORD. Anyone holding it can post attendance
# punches into the system.
# ============================================================

# The API is now a single origin. The old api-hris.brandigade.com
# split backend is retired.
HRIS_API_URL=https://hris.brandigade.com/api

# Must match the server exactly - no quotes, no trailing spaces.
SYNC_AGENT_TOKEN=9cdc165468a0e9f7f2e3a671c1e3dd55a15228208612276999255d1de623686c

# >>> SET THIS <<< the biometric device IP on the office LAN.
# The laptop running this agent must be on the same network.
ZKTECO_IP=192.168.18.201
ZKTECO_PORT=4370
ZKTECO_TIMEOUT_MS=10000

# Days of punches read per run. The overlap is deliberate - the
# server upserts, so re-sending is harmless and covers missed runs.
LOOKBACK_DAYS=3

# Window used by `npm run full-sync` for the initial backfill.
FULL_LOOKBACK_DAYS=60

BATCH_SIZE=2000
